import { SertARienDirective } from './sert-arien.directive';

describe('SertARienDirective', () => {
  it('should create an instance', () => {
    const directive = new SertARienDirective();
    expect(directive).toBeTruthy();
  });
});
