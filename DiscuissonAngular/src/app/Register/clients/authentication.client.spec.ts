import { AuthenticationClient } from './authentication.client';

describe('AuthenticationClient', () => {
  it('should create an instance', () => {
    expect(new AuthenticationClient()).toBeTruthy();
  });
});
